
<nav class="navbar navbar-expand-lg navbar-dark bg-info ps-4">
        <a class="navbar-brand" href="{{path('homepage')}}">MyBooking</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
          <ul class="navbar-nav mr-auto">
                <li class="nav-item active ">
                  <a class="nav-link" href="{{path('admin_dashboard')}}"><i class="fas fa-chart-line"></i> Tableau de bord&nbsp;</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="{{path('app_admin_ads_list')}}"><i class="fas fa-hotel"></i> Annonces&nbsp;</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_bookings_list')}}"><i class="fas fa-folder"></i> Réservations&nbsp;</a>
                  </li>

                  <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_comment_list')}}"><i class="fas fa-comments"></i> Commentaires&nbsp;</a>
                  </li>
           </ul>


      <ul class="navbar-nav ms-auto align-items-center">

        {% if app.user is not null %}
        
              <li class="nav-item">
                  <small><a href="{{path('homepage')}}" class="nav-link" target="_blank"><i class="fas fa-eye"></i> Voir le site&nbsp;</a></small>
              </li>

                <li class="nav-item dropdown">
                      <a href="#" class="nav-link dropdown-toggle" id="accountDropdownLink" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="true"> 
                          <img src="{{app.user.avatar}}" alt="Avatar de {{ app.user.fullName }}" class="avatar avatar-small">
                            {{app.user.fullName}}
                      </a>

                      <div class="dropdown-menu-right dropdown-menu mr-8" aria-labelledby="accountDropdownLink">
                              <a class="dropdown-item" href="{{path('admin_account_logout')}}">Deconnexion</a>  
                      </div>
                </li>


          {% else %}
          <li class="nav-item">
              <a class="nav-link" href="{{path('account_register')}}">Inscription</a>
          </li>
          
          <li class="nav-item">
              <a class="nav-link" href="{{path('account_login')}}">Connexion</a>
          </li>
          {% endif %}
      </ul> 
    </div>
</nav>
